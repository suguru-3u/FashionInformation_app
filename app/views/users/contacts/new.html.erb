<div class="user_contact">
	<div class="container mt-5 pt-5">
		<h2 class = "pb-3 pt-3 text-center">お問い合わせ</h2>
		<p class = 'text-center'>
			このページでは運営にお問い合わせを行うことができます。<br>
			退会希望者の方はこちらからお願い致します。
		</p>

		<%= form_for(@contact,url: users_contacts_path, method: :post) do |f| %>

		<!-- エラーメッセージ表示-->
		<%= render partial: "users/shared/error", locals: { user: @contact }%>

		<!-- ユーザー情報表示-->
		<div class="mt-4">
			<%= f.label :'タイトル' %><br>
			<%= f.text_field :title,class:'form-control' %>
		</div>

		<div class="mt-4">
			<%= f.label :'本文' %><br/>
			<%= f.text_area :contact_body,class:'form-control' %>
		</div>

		<div class="mt-4">
			<%= f.submit "送信",class: 'btn btn-outline-primary'%>
		</div>
		<% end %>
	</div>
</div>
